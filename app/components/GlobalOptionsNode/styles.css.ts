import { style } from "@vanilla-extract/css";

export const topDiv = style({
    display: "flex",
    flexDirection: "row",
});

export const title = style({ background: "#424242", color: "#ffffff" });
